services:
  - type: web
    name: Intellimock-Backend
    env: node
    buildCommand: npm install && npx prisma migrate deploy
    startCommand: tsx src/server.ts
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: intellimock_db_test
          property: connectionString
